import React from 'react';
import './index.css'; // Optional: if you want to separate the styles

const Testimonials = () => {
  return (
    <section className="portfolio">
      <div className="container">
        <header className="section-header">
          <h3 className="section-title">TESTIMONIALS</h3>
          <p>
            "Highlighting authentic testimonials, expert reviews, industry certifications, and prestigious recognitions that validate WellTrack's impact in digital healthcare and self-care innovation."
          </p>
        </header>
        {/* You can add testimonial cards or sliders here later */}
      </div>
    </section>
  );
};

export default Testimonials;
